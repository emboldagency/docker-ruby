
#######################################
# Ruby
#######################################

mkdir -p $GEM_HOME \
    && gem install bundler colorls /coder/pulsar.gem --conservative \
    && cd ~/pulsar \
    && bundle install
